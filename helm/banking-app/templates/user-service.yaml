apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: {{ include "banking-app.namespace" . }}
  labels:
    app: user-service
spec:
  replicas: {{ .Values.userService.replicas }}
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: "{{ .Values.ecrRegistry }}/{{ .Values.userService.image.repository }}:{{ .Values.imageTag }}"
        ports:
        - containerPort: 8000
        resources:
          requests:
            memory: "{{ .Values.userService.resources.requests.memory }}"
            cpu: "{{ .Values.userService.resources.requests.cpu }}"
          limits:
            memory: "{{ .Values.userService.resources.limits.memory }}"
            cpu: "{{ .Values.userService.resources.limits.cpu }}"
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: banking-secrets
              key: DATABASE_URL
        - name: FRONTEND_ORIGIN
          valueFrom:
            configMapKeyRef:
              name: banking-config
              key: FRONTEND_ORIGIN
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ include "banking-app.namespace" . }}
spec:
  selector:
    app: user-service
  ports:
    - port: {{ .Values.userService.service.port }}
      targetPort: 8000
  type: ClusterIP
