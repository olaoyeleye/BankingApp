---
apiVersion: v1
kind: Service
metadata:
  name: bank-app-service
  namespace: default
spec:
  selector:
    app: bank-app
  type: LoadBalancer      # gives you an external AWS ELB
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000